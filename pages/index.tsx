import React from 'react';
import App from '../src/App';
import { NextPage } from 'next';

const Home: NextPage = (props) => {
  return (
    <main>
      <App/>
    </main>
  );
};

export default Home;
